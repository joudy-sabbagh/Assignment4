@using Application.DTOs
@model Presentation.Models.PagedListViewModel<VenueListDTO>

    @{
    ViewData["Title"] = "Venues";
    }

    <h1>Venues</h1>

    <table class="table table-striped">
        <thead>
            <tr><th>Name</th><th>Location</th><th></th></tr>
        </thead>
        <tbody>
            @foreach (var v in Model.Items)
    {
            <tr>
                <td>@v.Name</td>
                <td>@v.Location</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@v.Id" class="btn btn-sm btn-secondary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@v.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>

    <nav aria-label="Venue pages">
        <ul class="pagination">
            @for (int p = 1; p <= Model.TotalPages; p++)
    {
            <li class="page-item @(p == Model.PageNumber ? "active" : "")">
                <a class="page-link" asp-action="Index" asp-route-page="@p">@p</a>
            </li>
    }
        </ul>
    </nav>

    <a asp-action="Create" class="btn btn-success">Create New Venue</a>
